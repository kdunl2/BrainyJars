"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var countDecimalPlaces = function countDecimalPlaces(num) {
  if (!Number.isFinite(num)) {
    return 0;
  }

  var epsilon = 1;
  var precision = 0;

  while (Math.round(num * epsilon) / epsilon !== num) {
    epsilon *= 10;
    precision += 1;
  }

  return precision;
};

var _default = countDecimalPlaces;
exports["default"] = _default;